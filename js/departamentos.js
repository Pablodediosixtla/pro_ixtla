(()=>{const grid=document.getElementById('departmentGrid'),search=document.getElementById('departmentSearch'),modal='departmentModal',form=document.getElementById('departmentForm');let data=[];async function load(){try{data=await App.api('db/web/departamentos/list.php?all=1&q='+encodeURIComponent(search?.value||''));grid.innerHTML=data.length?data.map(d=>`<article class="department-card"><div class="department-card-head"><span class="dept-icon" style="--dept:${d.color_hex}">🏛</span><span class="badge ${d.estatus==='ACTIVO'?'success':'neutral'}">${d.estatus}</span></div><div><small>${d.codigo}${d.es_tesoreria?' · TESORERÍA':''}</small><h3>${d.nombre}</h3><p>${d.descripcion||'Sin descripción'}</p></div><div class="department-stats"><span><b>${d.usuarios_count}</b><small>usuarios</small></span></div><button class="btn btn-soft" data-edit="${d.departamento_id}">Editar</button></article>`).join(''):'<div class="empty-state">No hay departamentos.</div>'}catch(e){App.toast(e.message,'error')}}function open(d={}){form.reset();form.departamento_id.value=d.departamento_id||'';form.codigo.value=d.codigo||'';form.nombre.value=d.nombre||'';form.descripcion.value=d.descripcion||'';form.color_hex.value=d.color_hex||'#859F8E';form.icono.value=d.icono||'building';form.es_tesoreria.checked=Number(d.es_tesoreria||0)===1;form.estatus.value=d.estatus||'ACTIVO';document.getElementById('departmentModalTitle').textContent=d.departamento_id?'Editar departamento':'Nuevo departamento';App.openModal(modal)}grid?.addEventListener('click',e=>{const id=e.target.dataset.edit;if(id)open(data.find(x=>String(x.departamento_id)===id))});document.getElementById('newDepartmentBtn')?.addEventListener('click',()=>open());form?.addEventListener('submit',async e=>{e.preventDefault();const o=Object.fromEntries(new FormData(form));o.es_tesoreria=form.es_tesoreria.checked?1:0;try{await App.api('db/web/departamentos/save.php',{method:'POST',body:o});App.closeModals();App.toast('Departamento guardado');load()}catch(err){App.toast(err.message,'error')}});search?.addEventListener('input',App.debounce(load));load()})();
