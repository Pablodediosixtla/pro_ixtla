(()=>{const y=document.getElementById('yearSelect');App.years(y);async function load(){try{const d=await App.api('db/web/dashboard/resumen.php?year='+y.value);document.getElementById('kpiAssigned').textContent=App.money(d.totals.asignado);document.getElementById('kpiEntries').textContent=App.money(d.totals.entradas);document.getElementById('kpiOutputs').textContent=App.money(d.totals.salidas);document.getElementById('kpiAvailable').textContent=App.money(d.totals.disponible);document.getElementById('pendingRequests').textContent=d.pending_requests;document.getElementById('openClarifications').textContent=d.open_clarifications;renderMonths(d.monthly);renderDeps(d.departments)}catch(e){App.toast(e.message,'error')}}function renderMonths(m){const el=document.getElementById('monthlyChart'),vals=Object.values(m).flatMap(x=>[x.entrada,x.salida]),max=Math.max(1,...vals);const names=['Ene','Feb','Mar','Abr','May','Jun','Jul','Ago','Sep','Oct','Nov','Dic'];el.innerHTML=names.map((n,i)=>{const x=m[i+1]||{entrada:0,salida:0};return `<div class="month-group"><div class="bars"><i class="bar entry" style="height:${Math.max(3,x.entrada/max*100)}%" title="${App.money(x.entrada)}"></i><i class="bar output" style="height:${Math.max(3,x.salida/max*100)}%" title="${App.money(x.salida)}"></i></div><small>${n}</small></div>`}).join('')}function renderDeps(ds){const el=document.getElementById('departmentSummary');el.innerHTML=ds.length?ds.map(d=>`<div class="summary-item"><div><div class="summary-title"><span class="dept-dot" style="background:${d.color_hex}"></span><b>${d.nombre}</b><strong>${d.ejercido_pct}%</strong></div><div class="progress"><span style="width:${Math.min(100,d.ejercido_pct)}%"></span></div><small>${App.money(d.disponible)} disponible</small></div></div>`).join(''):'<div class="empty-state">Sin departamentos en tu alcance.</div>'}y?.addEventListener('change',load);load()})();
