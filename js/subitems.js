(()=>{const table=document.getElementById('subitemTable'),form=document.getElementById('subitemForm'),depSel=document.getElementById('subitemDepartment');let data=[],deps=[];async function boot(){try{deps=await App.api('db/web/departamentos/list.php?all=1');depSel.innerHTML='<option value="">Global / Todos</option>'+deps.map(d=>`<option value="${d.departamento_id}">${d.nombre}</option>`).join('');await load()}catch(e){App.toast(e.message,'error')}}async function load(){try{data=await App.api('db/web/subitems/list.php?all=1');table.innerHTML=data.map(s=>`<tr><td><code>${s.codigo}</code></td><td><b>${s.nombre}</b><small class="cell-note">${s.descripcion||''}</small></td><td>${s.departamento||'Global'}</td><td><span class="badge ${s.estatus==='ACTIVO'?'success':'neutral'}">${s.estatus}</span></td><td><button class="btn btn-soft btn-sm" data-edit="${s.subitem_id}">Editar</button></td></tr>`).join('')}catch(e){App.toast(e.message,'error')}}function open(s={}){form.reset();form.subitem_id.value=s.subitem_id||'';form.codigo.value=s.codigo||'';form.nombre.value=s.nombre||'';form.departamento_id.value=s.departamento_id||'';form.descripcion.value=s.descripcion||'';form.estatus.value=s.estatus||'ACTIVO';App.openModal('subitemModal')}table?.addEventListener('click',e=>{const id=e.target.dataset.edit;if(id)open(data.find(x=>String(x.subitem_id)===id))});document.getElementById('newSubitemBtn')?.addEventListener('click',()=>open());form?.addEventListener('submit',async e=>{e.preventDefault();try{await App.api('db/web/subitems/save.php',{method:'POST',body:Object.fromEntries(new FormData(form))});App.closeModals();App.toast('Sub-item guardado');load()}catch(err){App.toast(err.message,'error')}});boot()})();
